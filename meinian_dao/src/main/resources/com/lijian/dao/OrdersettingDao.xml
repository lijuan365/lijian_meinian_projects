<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lijian.dao.OrdersettingDao">
    <!--根据预约日期进行查询    -->
    <select id="findCountByOrderDate" parameterType="java.util.Date" resultType="long">
        select count(1)  from t_ordersetting where orderDate  = #{orderDate}
    </select>
    <!--新增-->
    <insert id="add" parameterType="orderSetting">
        insert into t_ordersetting(orderDate,number,reservations)values(#{orderDate},#{number},#{reservations});
    </insert>
    <!-- 根据预约日期更新的预约的人数   -->
    <update id="EditOrderNumberByOrderDate" parameterType="orderSetting">
        update t_ordersetting set number = #{number} where orderDate=#{orderDate}
    </update>
    <!-- 根据月份获取预约设置 -->
    <select id="getOrderSettingByMonth" parameterType="hashmap" resultType="orderSetting">
       select orderDate,number,reservations from t_ordersetting where orderDate between #{dateBegin} and #{dateEnd}
    </select>
    <!--根据预约日期查询预约设置的信息-->
    <select id="findByOrderDate" parameterType="date" resultType="orderSetting">
         select * from t_ordersetting where orderDate = #{orderDate}
    </select>
    <!-- 根据日期更新预约人数   -->
    <update id="editNumberByDate" parameterType="orderSetting">
        update t_ordersetting set number = #{number} where orderDate = #{orderDate}
    </update>
    <!--   根据日期编辑记录 -->
    <update id="editReservationsByOrderDate" parameterType="orderSetting">
       update t_ordersetting set reservations = #{reservations} where orderDate = #{orderDate}
    </update>
</mapper>

